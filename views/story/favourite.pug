extends ../layout.pug

block title 
  title="Danh sách truyện yêu thích"

block content 
  if favouriteStories.length>0
    style.
      .favourite-story-image{
        object-fit: cover;
        width: 100%;
        max-width: 200px;
        height: 200px;
      }
    h2.text-center.text-primary.mb-4.mt-2 Danh sách truyện yêu thích
    each story in favouriteStories 
      .row.border.border-primary.m-2(onclick=`window.location.assign("/story/overview?id=${story.id}")`) 
        .col-sm-2.d-flex.justify-content-center 
          img.favourite-story-image(src=`${story.image_path}`)
        .col-8
          h2.text-primary= story.name 
          h4.text-warning= story.author 
          .d-flex.justify-content-start 
            h3= story.rate 
            span.ml-2.fa.fa-2x.fa-star.text-warning
  else
    style.
      #empty-favourite{
        background-image: url("/images/not-found-bg.jpg");
        background-size: cover;
        width: 50%;
        min-height: 100vh;
        margin: auto;
      }
    h2.text-center.text-primary.mt-2 Danh sách truyện yêu thích
    #empty-favourite
      .d-flex.justify-content-center
        h4.text-center.text-warning Bạn chưa có truyện nào trong danh sách yêu thích, các truyện được đưa vào danh sách yêu thích là những truyện được bạn đánh giá từ 4 sao trở lên